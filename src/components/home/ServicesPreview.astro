---
import { services } from '../../data/services';
---

<section class="services-section section-pad">
  <div class="container">
    <div class="section-header section-header--split reveal">
      <div class="section-header__left">
        <span class="text-eyebrow">02 / Expertise</span>
        <h2 class="text-h2">What We Do</h2>
        <span class="section-header__line"></span>
      </div>
      <div class="section-header__right">
        <p class="section-desc">From concept to completion, we deliver construction excellence across every sector.</p>
      </div>
    </div>

    <div class="services-list" id="services-list">
      {services.slice(0, 5).map((service, index) => (
        <a href="/services" class="service-row interactive reveal" data-service-index={index} style={`transition-delay: ${index * 0.08}s;`}>
          <div class="service-row__inner">
            <div class="service-number">0{index + 1}</div>
            <div class="service-title-wrap">
              <h3 class="service-title text-h3">{service.title}</h3>
              <p class="service-desc">{service.description}</p>
            </div>
            <div class="service-arrow">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M5 12H19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                <path d="M12 5L19 12L12 19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
          </div>
          <div class="service-image-preview" aria-hidden="true">
            <img src={service.image} alt="" loading="lazy" draggable="false" />
          </div>
        </a>
      ))}
    </div>

    <div class="services-footer reveal" style="transition-delay: 0.4s;">
      <a href="/services" class="btn btn-outline magnetic-btn">
        VIEW ALL SERVICES
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <path d="M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          <path d="M12 5L19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .services-section {
    background-color: var(--c-white);
    position: relative;
  }

  .section-header .text-h2 {
    margin-top: 0.75rem;
  }

  .services-list {
    border-top: 1px solid var(--c-grey-light);
    position: relative;
  }

  .service-row {
    display: block;
    border-bottom: 1px solid var(--c-grey-light);
    position: relative;
    text-decoration: none;
    color: inherit;
  }

  .service-row__inner {
    display: grid;
    grid-template-columns: 48px 1fr auto;
    align-items: center;
    gap: 1.25rem;
    padding: 1.35rem 1rem;
    position: relative;
    z-index: 2;
    min-height: 76px;
    transition: background-color 0.35s var(--ease-out-expo);
  }

  .service-row:hover .service-row__inner {
    background-color: var(--c-soft-white);
  }

  .service-number {
    font-family: var(--font-heading);
    font-size: 0.8rem;
    color: var(--c-grey);
    transition: color 0.3s ease;
    font-variant-numeric: tabular-nums;
  }

  .service-row:hover .service-number {
    color: var(--c-accent);
  }

  .service-title-wrap {
    min-width: 0;
  }

  .service-title {
    margin: 0;
    font-size: clamp(1.15rem, 2.5vw, 1.65rem);
    font-weight: 500;
    letter-spacing: -0.02em;
    transition: color 0.3s ease;
    display: inline;
    background-image: linear-gradient(var(--c-accent), var(--c-accent));
    background-size: 0% 2px;
    background-repeat: no-repeat;
    background-position: 0 100%;
    transition: background-size 0.4s var(--ease-out-expo);
  }

  .service-row:hover .service-title {
    background-size: 100% 2px;
  }

  .service-desc {
    display: none;
  }

  .service-arrow {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    border: 1px solid var(--c-grey-light);
    transition: all 0.4s var(--ease-out-expo);
    flex-shrink: 0;
    background: var(--c-white);
  }

  .service-row:hover .service-arrow {
    background-color: var(--c-charcoal);
    color: var(--c-white);
    border-color: var(--c-charcoal);
    transform: rotate(-45deg);
  }

  /* Square floating preview — center clamped inside row, image can overflow */
  .service-image-preview {
    position: fixed;
    top: 0;
    left: 0;
    width: 220px;
    height: 220px;
    pointer-events: none;
    z-index: 15;
    opacity: 0;
    visibility: hidden;
    transform: translate(-50%, -50%) scale(0.88);
    transition: opacity 0.35s var(--ease-out-expo), visibility 0.35s, transform 0.35s var(--ease-out-expo);
    will-change: left, top;
    overflow: hidden;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.18);
  }

  .service-image-preview.visible {
    opacity: 1;
    visibility: visible;
    transform: translate(-50%, -50%) scale(1);
  }

  .service-image-preview img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .services-footer {
    margin-top: 3rem;
    display: flex;
    justify-content: flex-end;
  }

  @media (min-width: 1024px) {
    .service-row__inner {
      grid-template-columns: 56px 1fr auto;
      gap: 1.5rem;
      padding: 1.5rem 1.25rem;
      min-height: 84px;
    }

    .service-image-preview {
      width: 260px;
      height: 260px;
    }
  }

  @media (max-width: 1023px) {
    .service-image-preview {
      display: none;
    }

    .service-desc {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      font-size: 0.8rem;
      color: var(--c-grey);
      line-height: 1.5;
      margin-top: 0.35rem;
    }
  }

  @media (max-width: 768px) {
    .service-row__inner {
      grid-template-columns: 36px 1fr auto;
      gap: 0.75rem;
      padding: 1.15rem 0.75rem;
      min-height: auto;
    }

    .service-arrow {
      width: 34px;
      height: 34px;
    }

    .services-footer {
      justify-content: center;
    }
  }
</style>

<script>
  if (window.matchMedia('(pointer: fine)').matches && window.innerWidth >= 1024) {
    const rows = document.querySelectorAll('.service-row');

    rows.forEach(row => {
      const preview = row.querySelector('.service-image-preview') as HTMLElement;
      if (!preview) return;

      let targetX = 0;
      let targetY = 0;
      let currentX = 0;
      let currentY = 0;
      let isHovering = false;
      let rafId: number;

      const lerp = (start: number, end: number, factor: number) => start + (end - start) * factor;

      /** Clamp image CENTER to stay inside the row bounds */
      const clampCenterToRow = (clientX: number, clientY: number) => {
        const rect = row.getBoundingClientRect();
        return {
          x: Math.max(rect.left, Math.min(rect.right, clientX)),
          y: Math.max(rect.top, Math.min(rect.bottom, clientY)),
        };
      };

      const animate = () => {
        currentX = lerp(currentX, targetX, 0.12);
        currentY = lerp(currentY, targetY, 0.12);

        preview.style.left = `${currentX}px`;
        preview.style.top = `${currentY}px`;

        if (isHovering) {
          rafId = requestAnimationFrame(animate);
        }
      };

      row.addEventListener('mouseenter', (e: MouseEvent) => {
        isHovering = true;
        const clamped = clampCenterToRow(e.clientX, e.clientY);
        targetX = clamped.x;
        targetY = clamped.y;
        currentX = targetX;
        currentY = targetY;
        preview.style.left = `${currentX}px`;
        preview.style.top = `${currentY}px`;
        preview.classList.add('visible');
        animate();
      });

      row.addEventListener('mousemove', (e: MouseEvent) => {
        const clamped = clampCenterToRow(e.clientX, e.clientY);
        targetX = clamped.x;
        targetY = clamped.y;
      });

      row.addEventListener('mouseleave', () => {
        isHovering = false;
        cancelAnimationFrame(rafId);
        preview.classList.remove('visible');
      });
    });
  }
</script>
